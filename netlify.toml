[build]
  command = "npx prisma generate && npx prisma db push && npx tsx prisma/seed.ts && next build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
